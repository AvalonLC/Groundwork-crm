{
  "version": 1,
  "note": "Seed values for a new tenant_finance_policy row. materiality_threshold_cents defaults from approval-thresholds.json's default_materiality_threshold_cents at seed time — duplicated here as an explicit override point per tenant type, not a second source of truth (tenant-onboarding code should prefer approval-thresholds.json unless a tenant type below is selected).",

  "equipment_engine_active": false,
  "materiality_threshold_cents": 50000,
  "restated_target_cents": 0,
  "black_friday_date": null
}
